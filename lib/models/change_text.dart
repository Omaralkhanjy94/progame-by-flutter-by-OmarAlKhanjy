import 'package:flutter/cupertino.dart';
import "package:programee2/screens/first_game.dart";

class ChangeText extends StatelessWidget {
  const ChangeText({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
